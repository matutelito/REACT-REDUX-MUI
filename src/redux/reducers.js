
const rootReducer = (state ={}, action) =>{

    // if (action.type === GET_CLIENT){
    //     return {
    //         ...state,
    //         casos: action.casos
    //     }
    // }

    // else if (action.type === DELETE_OFF_CASOS){
    //     return {
    //         ...state,
    //         casos: state.casos.filter(caso => caso.ID !== action.ID) 
    //     }
    // }
    return state
}

export default rootReducer; 